[package]
name = "version-core"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1"
itertools = "0.14.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
version-api-macros = { path = "../version-api-macros" }
version-id = { path = "../version-id" }
